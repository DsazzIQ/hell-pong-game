export default abstract class Component {
  protected constructor() {
    // You can add any shared properties or methods for all components here.
    // However, in most cases, the Component class can be left empty.
  }
}