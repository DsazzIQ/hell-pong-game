/**
 * BALL_SIZE: The size of the ball (pixels).
 * BALL_SPEED: The velocity speed
 */
export const BALL_SIZE = 28;
export const BALL_SPEED = 120;

/**
 * PADDLE_SPEED: The velocity speed
 * PADDLE_WIDTH: The width of the paddle (pixels).
 * PADDLE_HEIGHT: The height of the paddle (pixels).
 * PADDLE_OFFSET: The horizontal distance from the left and right edges of the game to the paddles (pixels).
 */
export const PADDLE_SPEED = 200;
export const PADDLE_WIDTH = 18;
export const PADDLE_HEIGHT = 62;
export const PADDLE_OFFSET = 30;

export const MAX_ROOM_PLAYERS = 2;

export const GAME_WIDTH = 800;
export const GAME_HEIGHT = 600;

// export const GAME_FPS = 30;
export const CLIENT_FPS = 40;
export const CLIENT_UPDATE_INTERVAL = 1000 / CLIENT_FPS;

export const SERVER_FPS = 30;
export const SERVER_UPDATE_INTERVAL = 1000 / SERVER_FPS;

