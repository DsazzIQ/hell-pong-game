{
  "name": "my-project",
  "version": "1.0.0",
  "description": "A multiplayer ping pong game built with Phaser 3 and Socket.IO",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build:shared": "cd shared && tsc",
    "dev": "concurrently \"yarn dev:client\" \"yarn dev:server\"",
    "dev:client": "cd client && yarn dev",
    "dev:server": "yarn build:shared && cd server && yarn dev"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^6.2.2"
  }
}